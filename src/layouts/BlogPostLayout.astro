---
import BaseLayout from './BaseLayout.astro';

interface Props {
  title: string;
  date: Date;
  category: string;
  description: string;
  tags: string[];
}

const { title, date, category, description, tags } = Astro.props;

const formatted = new Intl.DateTimeFormat('en-US', {
  year: 'numeric', month: 'long', day: 'numeric',
}).format(date);

const categoryColors: Record<string, string> = {
  antennas: 'bg-emerald-400/10 text-emerald-400',
  ml:       'bg-violet-400/10 text-violet-400',
  rf:       'bg-sky-400/10 text-sky-400',
  optics:   'bg-amber-400/10 text-amber-400',
  dsp:      'bg-rose-400/10 text-rose-400',
  misc:     'bg-zinc-400/10 text-zinc-400',
};
---

<BaseLayout title={`${title} — frah90`} description={description}>
  <article class="max-w-3xl mx-auto px-6 py-16">
    <header class="mb-12 pb-10 border-b border-zinc-900">
      <div class="flex items-center gap-3 mb-6">
        <a
          href={`/blog/${category}`}
          class:list={['text-xs font-mono', categoryColors[category] ?? categoryColors.misc]}
        >
          {category}
        </a>
        <span class="text-zinc-800">·</span>
        <time class="text-xs text-zinc-600">{formatted}</time>
      </div>
      <h1 class="text-3xl sm:text-4xl text-zinc-100 leading-tight mb-4"
        style="font-family: 'Lora', Georgia, serif; font-weight: 400;">
        {title}
      </h1>
      <p class="text-base text-zinc-500 leading-relaxed" style="font-family: 'Lora', Georgia, serif; font-style: italic;">
        {description}
      </p>
    </header>

    <div class="prose prose-invert prose-zinc max-w-none
      prose-headings:text-zinc-100
      prose-p:text-zinc-300 prose-p:leading-relaxed
      prose-a:text-emerald-400 prose-a:no-underline hover:prose-a:underline
      prose-code:text-emerald-300 prose-code:bg-zinc-800/80 prose-code:px-1.5 prose-code:rounded prose-code:text-sm
      prose-pre:bg-zinc-900 prose-pre:border prose-pre:border-zinc-800
      prose-strong:text-zinc-200
      prose-li:text-zinc-300"
      style="font-family: 'Lora', Georgia, serif;">
      <slot />
    </div>

    <div class="mt-16 pt-8 border-t border-zinc-900">
      <a href="/blog" class="text-sm text-zinc-500 hover:text-zinc-200 transition-colors">
        ← Writing
      </a>
    </div>
  </article>
</BaseLayout>
